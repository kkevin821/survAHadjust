% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aiptw_adjustedsurv.R
\name{aiptw_adjustedsurv}
\alias{aiptw_adjustedsurv}
\title{Estimate adjusted survival curves using AIPTW}
\usage{
aiptw_adjustedsurv(
  data,
  covars_treat,
  covars_outcome,
  covars_cens = NULL,
  y_col = "time",
  delta_col = "event",
  z_col = "group",
  bootstrap = FALSE,
  n_boot = 300,
  ps_clip = c(1e-06, 1 - 1e-06),
  g_clip = 1e-06
)
}
\arguments{
\item{data}{A data frame.}

\item{covars_treat}{Character vector of covariates for the treatment model.}

\item{covars_outcome}{Character vector of covariates for the outcome model.}

\item{covars_cens}{Character vector of covariates for the censoring model.
If \code{NULL}, pooled KM is used for censoring. If not \code{NULL}, a Cox model is
used for censoring.}

\item{y_col}{Name of the observed time variable. Default is \code{"time"}.}

\item{delta_col}{Name of the event indicator variable. Default is \code{"event"}.}

\item{z_col}{Name of the treatment/group variable. This variable should be
coded as 0/1. Default is \code{"group"}.}

\item{bootstrap}{Logical. If \code{TRUE}, bootstrap adjusted survival curves are
generated and stored in \code{boot_data}.}

\item{n_boot}{Number of bootstrap samples.}

\item{ps_clip}{Truncation range for propensity scores.}

\item{g_clip}{Lower bound for censoring survival probabilities.}
}
\value{
An adjusted survival curve object. The main element is \code{adj},
a data frame with columns \code{time}, \code{surv}, and \code{group}. If
\code{bootstrap = TRUE}, the object also contains \code{boot_data}.
}
\description{
Estimates adjusted survival curves using augmented inverse probability of
treatment weighting (AIPTW). This implementation follows the AIPTW/IPCW
setup used in the simulation study and application.
}
\examples{
D <- survival::pbc[!is.na(survival::pbc$trt), ]
D$Y <- D$time / 365.25
D$Delta <- as.numeric(D$status == 2)
D$Z <- as.numeric(D$trt == 2)

D <- D[complete.cases(D[, c("Y", "Delta", "Z", "age", "sex", "bili", "albumin")]), ]

adjsurv <- aiptw_adjustedsurv(
  data = D,
  covars_treat = c("age", "sex"),
  covars_outcome = c("bili", "age", "albumin"),
  y_col = "Y",
  delta_col = "Delta",
  z_col = "Z",
  bootstrap = FALSE
)

adjusted_ahsw(adjsurv, to = 5, conf_int = FALSE)

\dontrun{
adjsurv_boot <- aiptw_adjustedsurv(
  data = D,
  covars_treat = c("age", "sex"),
  covars_outcome = c("bili", "age", "albumin"),
  y_col = "Y",
  delta_col = "Delta",
  z_col = "Z",
  bootstrap = TRUE,
  n_boot = 300
)

adjusted_ahsw(adjsurv_boot, to = 5, conf_int = TRUE)
}

}
\references{
Ozenne B, Scheike TH, Staerk L, and Gerds TA. On the estimation of average
treatment effects with right-censored time to event outcome and competing
risks. Biometrical Journal. 2020;62(3):751-763.
}
